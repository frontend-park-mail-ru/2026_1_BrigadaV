export type CategorySummaryDTO = {
    id: number;
    name: string;
    description: string;
};

export type CategoryDTO = {
    ID: number;
    Name: string;
    Description: string;
    ApplicableTypes: string[];
};
